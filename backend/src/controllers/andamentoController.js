/**
 * Dentro da arquitetura proposta o controller deverá ser enxuto,
 * receber req, chamar o service, devolver res e passar o erro pra next
 * 
 * Principais funções:
 *  - Lê req.params, req.body, req.query;
 *  - Chama o service correto
 *  - Retorna JSON;
 *  - SEM REGRAS DE NEGÓCIO
 *  - SEM PRISMA DIRETO
 */
